---
name: metric-validation
description: 对报告中的数量、比例、排名、趋势和时间窗口进行确定性核对。
---

# 指标校验

## 规则

- 所有计算由工具或代码完成，LLM 只解释结果。
- 明确分子、分母、去重键、时间范围、时区和缺失值处理。
- 同比/环比必须存在可比周期和一致口径。
- 排名必须说明排序字段和并列处理。
- 总数必须能由分项复算；无法对齐时停止发布。

## 输出

记录每个指标的名称、值、单位、公式、源字段、源数据引用和校验状态。管理摘要中的数字必须引用已校验指标。
