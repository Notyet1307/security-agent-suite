# v0.2.0 Runtime Foundation — Published History

Published to GitHub Issues #13, #14, and #61 on 2026-09-09.

The GitHub Issues are authoritative for task state. This file is historical and is not synchronized after publication.

## Spec

`docs/specs/mvp.md` (working-tree baseline; no Git commit version at publication time).

## Published mapping

- `SAS-106` → GitHub #13
- `SAS-107` → GitHub #14
- `SAS-108` → GitHub #61

## Scope

Runtime foundation only: cancellation contract, fail-closed error/retry boundaries, and separately authorized live Sandbox acceptance.

## Gate

Offline `make verify`, `make race`, and `make smoke` remain required. Live work requires explicit authorization, fresh credentials, independent acceptance, and real runtime provenance.
