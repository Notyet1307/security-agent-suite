# Issue Backlog Map

GitHub is the authoritative task tracker. `planning/backlog.tsv` is the dependency and acceptance index; GitHub Issues carry execution discussion and state.

## Current release
`v0.2.0-runtime-foundation`: SAS-106 → SAS-107 → SAS-108, with SAS-108 also depending on SAS-104 and SAS-105.

## Rules
Select one release slice at a time. Reconcile this map with GitHub before each release. Do not duplicate task status in `docs/work/current.md`.
